{
  "name": "maya-webhook",
  "version": "1.0.0",
  "private": true,
  "description": "Bare-bones Twilio + web simulator webhook for Maya HVAC demo. Rate-limited via Upstash Redis, talks to Claude Sonnet directly.",
  "engines": { "node": ">=20" },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.0",
    "@upstash/redis": "^1.34.0",
    "twilio": "^5.0.0"
  }
}
